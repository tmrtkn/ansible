all:
  hosts:
    laidun:
  children:
    local:
      hosts:
        lab:
        deploy:
    dns:
      hosts:
        lab:
    docker:
      hosts:
        pihole:
        camera:
  vars:
    ansible_user: "{{ username }}"
    ansible_ssh_pass: "{{ password }}"
    ansible_become_pass: "{{ password }}"
