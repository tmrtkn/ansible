all:
  hosts:
    laidun:
    # pilvilaidun:
  children:
    local:
      hosts:
        lab:
        deploy:
        pihole:
        camera:
    dns:
      hosts:
        lab:
    docker:
      hosts:
        lab:
    monitor_server:
      hosts:
        lab:
    vpn:
      hosts:
        lab:
        camera:
  vars:
    ansible_user: "{{ username }}"
    ansible_ssh_pass: "{{ password }}"
    ansible_become_pass: "{{ password }}"
